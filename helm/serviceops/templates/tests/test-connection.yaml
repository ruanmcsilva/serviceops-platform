apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "serviceops.fullname" . }}-test-connection"
  labels:
    {{- include "serviceops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "serviceops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
